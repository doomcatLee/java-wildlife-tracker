
<h2>Ranger details</h2>

<h1>$ranger.getName()</h1>
<h3>Contact: $ranger.getContact()</h3>
<h3>Badge Number: $ranger.getBadgeNumber()</h3>

<h4>Sightings submitted by $ranger.getName()</h4>

#foreach ($i in $sightings)
  #if ($i.getRangerId() == $ranger.getId())
    <ul>
      <li><strong>Reported date: </strong>$i.getSightTime()</li>
      <li><strong>Location: </strong> $i.getLocation().getName()</li>
    </ul>
  #end
#end
